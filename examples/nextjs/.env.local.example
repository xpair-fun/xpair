# Note: onchain.fi API key is stored server-side in /api/onchain/[endpoint].ts
# No environment variables needed for the client!
# The API key is never exposed to the browser for security.

# Helius RPC for Solana (already configured)
# NEXT_PUBLIC_HELIUS_API_KEY=your-helius-key-here
